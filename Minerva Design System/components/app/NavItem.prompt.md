Minerva sidebar nav row. Orange filled block with white Jaro label + hard shadow when active; grey label that fills navy on hover otherwise.

```jsx
<NavItem active>Dashboard</NavItem>
<NavItem onClick={() => go("Budget")}>Budget</NavItem>
```
