Minerva progress rail — inset track with a rounded colored fill. Use for budget usage, badge progress, pomodoro session completion.

```jsx
<ProgressBar value={68} tone="teal" showLabel />
<ProgressBar value={3} max={4} tone="orange" height={12} />
```
