Minerva text field — navy-outlined rounded input with a soft shadow, body copy in Lexend.

```jsx
<Input label="Task name" placeholder="Walk the cat" />
<Input icon={<SearchIcon/>} placeholder="Search…" hint="Press Enter to add" />
```
